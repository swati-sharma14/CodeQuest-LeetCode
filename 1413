class Solution {
public:
    int minStartValue(vector<int>& nums) {
        for(int i=1; i<nums.size(); i++){
            nums[i] += nums[i-1];
        }
        int a = *min_element(nums.begin(), nums.end());
        if(a>=1){
            return 1;
        }
        return 1-a;
    }
};
